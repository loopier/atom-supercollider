# atom-supercollider
Things to use supercollider in atom -- **WARNING!: This is not a package!**  Just useful stuff

This is a shortcut for atom's supercollider package that evaluates the current paragraph instead of only the line or selected text. Edit `~/.atom/init.coffee` and  `~/.atom/keymap.cson`
